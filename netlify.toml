[build]
  command = "rm -rf node_modules && npm install && npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION          = "18"
  CI                    = "false"
  NPM_CONFIG_PRODUCTION = "false"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200